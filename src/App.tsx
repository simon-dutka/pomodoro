import React from 'react';

// Components
import { Clock } from './components/index.tsx';

const app = () => {
    return (
        <div>
            <Clock />
        </div>
    );
};

export default app;
